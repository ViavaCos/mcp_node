{
  "name": "mcp-node-auth-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "最小 OIDC 授权服务器（TypeScript）：为 MCP 资源服务器颁发 JWT 访问令牌",
  "main": "src/server.ts",
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.19.2",
    "jose": "^6.2.12"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
