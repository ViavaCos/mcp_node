{
  "name": "mcp-node-demo-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LLM Agent 演示（TypeScript）：通过 MCP 工具基于 5 个接口做任意分析（未配密钥时跑脚本化降级）",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
